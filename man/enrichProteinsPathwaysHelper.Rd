% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment_functions.R,
%   R/func_general_enrichment.R
\name{enrichProteinsPathwaysHelper}
\alias{enrichProteinsPathwaysHelper}
\title{Helper function to perform protein pathway enrichment analysis for a single contrast}
\usage{
enrichProteinsPathwaysHelper(
  de_analysis_results,
  organism_taxid,
  min_gene_set_size = 4,
  max_gene_set_size = 200,
  p_val_thresh = 0.05,
  protein_p_val_thresh = 0.05,
  cache_dir = "cache",
  cache_file = "uniprot_annotations.RDS",
  output_dir = "proteins_pathways_enricher",
  use_cached = TRUE,
  protein_id_delimiter = ":",
  protein_id_column = "Protein.Ids"
)

enrichProteinsPathwaysHelper(
  de_analysis_results,
  organism_taxid,
  min_gene_set_size = 4,
  max_gene_set_size = 200,
  p_val_thresh = 0.05,
  protein_p_val_thresh = 0.05,
  cache_dir = "cache",
  cache_file = "uniprot_annotations.RDS",
  output_dir = "proteins_pathways_enricher",
  use_cached = TRUE,
  protein_id_delimiter = ":",
  protein_id_column = "Protein.Ids"
)
}
\arguments{
\item{de_analysis_results}{Output from deAnalysisWrapperFunction containing differential expression results}

\item{organism_taxid}{NCBI taxonomy ID for the organism (e.g., "9606" for human)}

\item{min_gene_set_size}{Minimum number of genes in a gene set (default: 4)}

\item{max_gene_set_size}{Maximum number of genes in a gene set (default: 200)}

\item{p_val_thresh}{P-value threshold for enrichment significance (default: 0.05)}

\item{protein_p_val_thresh}{P-value threshold for protein significance (default: 0.05)}

\item{cache_dir}{Directory to store cached UniProt data (default: "cache")}

\item{output_dir}{Directory for output files (default: "proteins_pathways_enricher")}

\item{use_cached}{Whether to use cached data if available (default: TRUE)}

\item{protein_id_delimiter}{Delimiter used in protein IDs (default: ":")}

\item{protein_id_column}{Name of the protein ID column (default: "Protein.Ids")}
}
\value{
A data frame containing enrichment results

A data frame containing enrichment results
}
\description{
This function performs pathway enrichment analysis on protein data using GO terms and gene symbols
downloaded from UniProt. The data is cached for future use to improve performance.

This function performs pathway enrichment analysis on protein data using GO terms and gene symbols
downloaded from UniProt. The data is cached for future use to improve performance.
}
