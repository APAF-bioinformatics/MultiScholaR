% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiomics_enrichment_functions.R
\name{printStringDbFunctionalEnrichmentBarGraph}
\alias{printStringDbFunctionalEnrichmentBarGraph}
\title{Generate a Bar Graph of STRING DB Functional Enrichment Results}
\usage{
printStringDbFunctionalEnrichmentBarGraph(input_table, word_limit = 10)
}
\arguments{
\item{input_table}{A data frame containing functional enrichment results.
It is expected to have the following columns:
\itemize{
\item \code{comparison}: Character, identifier for the comparison group.
\item \code{termDescription}: Character, description of the enriched term.
\item \code{enrichmentScore}: Numeric, the enrichment score for the term.
\item \code{falseDiscoveryRate}: Numeric, the False Discovery Rate for the term.
\item \code{genesMapped}: Numeric, the number of genes mapped to the term.
\item \code{category}: Character or Factor, the category of the enrichment (e.g., GO BP, KEGG).
}}
}
\value{
A \code{ggplot} object representing the enrichment bar graph.
}
\description{
This function takes a data frame of STRING DB enrichment results and
generates a faceted bar graph. The graph displays the enrichment score for
terms, with points overlaid indicating the -log10(falseDiscoveryRate)
(color) and the number of genes mapped (size). Results are faceted by
'category' and 'comparison'.
}
\examples{
\dontrun{
# Assume 'enrichment_results_df' is a data frame structured as described above
# For example:
# enrichment_results_df <- data.frame(
#   comparison = rep(c("GroupA_vs_Control", "GroupB_vs_Control"), each = 2),
#   termDescription = c("Immune response", "Metabolic process", "Immune response", "Cell cycle"),
#   enrichmentScore = c(2.5, 1.8, 3.1, 2.0),
#   falseDiscoveryRate = c(0.01, 0.045, 0.005, 0.02),
#   genesMapped = c(50, 30, 65, 40),
#   category = rep(c("GO Biological Process", "KEGG Pathway"), times = 2)
# )
#
# enrichment_plot <- printStringDbFunctionalEnrichmentBarGraph(enrichment_results_df)
# print(enrichment_plot)
}
}
