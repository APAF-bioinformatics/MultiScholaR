% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_proteins_functions.R,
%   R/func_general_plotting.R
\name{plotPcaHelper}
\alias{plotPcaHelper}
\title{Plot PCA Helper}
\usage{
plotPcaHelper(
  data,
  design_matrix,
  sample_id_column = "Sample_ID",
  grouping_variable = "group",
  shape_variable = NULL,
  label_column = NULL,
  title,
  geom.text.size = 11,
  ncomp = 2,
  cv_percentile = 0.9,
  ...
)

plotPcaHelper(
  data,
  design_matrix,
  sample_id_column = "Sample_ID",
  grouping_variable = "group",
  shape_variable = NULL,
  label_column = NULL,
  title,
  geom.text.size = 11,
  ncomp = 2,
  cv_percentile = 0.9,
  ...
)
}
\arguments{
\item{data}{A matrix or data frame of quantitative data, with features in rows and samples in columns.}

\item{design_matrix}{A data frame containing sample metadata.}

\item{sample_id_column}{The name of the column in \code{design_matrix} that identifies samples. Defaults to "Sample_ID".}

\item{grouping_variable}{The column in \code{design_matrix} used for coloring points in the PCA plot. Defaults to "group".}

\item{shape_variable}{An optional column in \code{design_matrix} to be used for the shape aesthetic of points.}

\item{label_column}{An optional column in \code{design_matrix} to be used for labeling points.}

\item{title}{The title of the plot.}

\item{geom.text.size}{The size of the text labels if \code{label_column} is provided. Defaults to 11.}

\item{ncomp}{The number of principal components to compute. Defaults to 2.}

\item{cv_percentile}{The percentile threshold for selecting features based on coefficient of variation. Defaults to 0.90 (top 10\% most variable features).}

\item{...}{Additional arguments passed to other methods (not currently used).}
}
\value{
A \code{ggplot} object representing the PCA plot.

A \code{ggplot} object representing the PCA plot.
}
\description{
This function performs a Principal Component Analysis (PCA) on the given data and generates a ggplot object for visualization.
It includes filtering steps to select the most variable features before running PCA.

This function performs a Principal Component Analysis (PCA) on the given data and generates a ggplot object for visualization.
It includes filtering steps to select the most variable features before running PCA.
}
\examples{
# Create dummy data
data <- matrix(rnorm(1000), nrow = 100, ncol = 10)
colnames(data) <- paste0("Sample", 1:10)
rownames(data) <- paste0("Protein", 1:100)

# Create dummy design matrix
design_matrix <- data.frame(
  Sample_ID = paste0("Sample", 1:10),
  group = rep(c("A", "B"), each = 5),
  batch = rep(c("X", "Y"), times = 5)
)

# Generate PCA plot
plotPcaHelper(
  data = data,
  design_matrix = design_matrix,
  title = "PCA of Dummy Data",
  grouping_variable = "group",
  shape_variable = "batch"
)

# Create dummy data
data <- matrix(rnorm(1000), nrow = 100, ncol = 10)
colnames(data) <- paste0("Sample", 1:10)
rownames(data) <- paste0("Protein", 1:100)

# Create dummy design matrix
design_matrix <- data.frame(
  Sample_ID = paste0("Sample", 1:10),
  group = rep(c("A", "B"), each = 5),
  batch = rep(c("X", "Y"), times = 5)
)

# Generate PCA plot
plotPcaHelper(
  data = data,
  design_matrix = design_matrix,
  title = "PCA of Dummy Data",
  grouping_variable = "group",
  shape_variable = "batch"
)

}
