% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/func_general_filemgmt.R,
%   R/func_general_helpers.R
\name{chooseBestProteinAccession_s3}
\alias{chooseBestProteinAccession_s3}
\title{Choose Best Protein Accession for Data Frame (S3 Version)}
\usage{
chooseBestProteinAccession_s3(
  data_tbl,
  protein_id_column,
  seqinr_obj,
  seqinr_accession_column = "uniprot_acc",
  delim = ";",
  replace_zero_with_na = TRUE,
  aggregation_method = "mean"
)

chooseBestProteinAccession_s3(
  data_tbl,
  protein_id_column,
  seqinr_obj,
  seqinr_accession_column = "uniprot_acc",
  delim = ";",
  replace_zero_with_na = TRUE,
  aggregation_method = "mean"
)
}
\arguments{
\item{data_tbl}{Data frame containing protein quantitative data}

\item{protein_id_column}{Character string specifying the protein ID column name}

\item{seqinr_obj}{Data frame with FASTA sequence information (aa_seq_tbl_final)}

\item{seqinr_accession_column}{Character string specifying the accession column in seqinr_obj}

\item{delim}{Character string delimiter used to separate protein groups (default: ";")}

\item{replace_zero_with_na}{Logical, whether to replace zeros with NA (default: TRUE)}

\item{aggregation_method}{Character string aggregation method ("mean", "median", "sum") (default: "mean")}
}
\value{
Data frame with cleaned protein IDs and aggregated values

Data frame with cleaned protein IDs and aggregated values
}
\description{
A simplified S3 version of chooseBestProteinAccession that works
directly on data frames. Resolves protein groups by selecting the best
representative protein based on FASTA sequence data and aggregates
quantitative values.

A simplified S3 version of chooseBestProteinAccession that works
directly on data frames. Resolves protein groups by selecting the best
representative protein based on FASTA sequence data and aggregates
quantitative values.
}
\details{
This function processes protein groups (e.g., "P12345;P67890;Q11111") by:
\itemize{
\item Splitting groups using the specified delimiter
\item Finding the best representative protein using FASTA sequence data
\item Aggregating quantitative values for the chosen protein
\item Returning cleaned data with single protein IDs
}

The selection of "best" protein is based on:
\itemize{
\item Presence in FASTA sequence database
\item Protein length (longer sequences preferred)
\item Alphabetical order as tiebreaker
}

This function processes protein groups (e.g., "P12345;P67890;Q11111") by:
\itemize{
\item Splitting groups using the specified delimiter
\item Finding the best representative protein using FASTA sequence data
\item Aggregating quantitative values for the chosen protein
\item Returning cleaned data with single protein IDs
}

The selection of "best" protein is based on:
\itemize{
\item Presence in FASTA sequence database
\item Protein length (longer sequences preferred)
\item Alphabetical order as tiebreaker
}
}
\examples{
\dontrun{
cleaned_data <- chooseBestProteinAccession_s3(
  data_tbl = protein_data,
  protein_id_column = "Protein.Group",
  seqinr_obj = aa_seq_tbl_final,
  seqinr_accession_column = "uniprot_acc"
)
}

\dontrun{
cleaned_data <- chooseBestProteinAccession_s3(
  data_tbl = protein_data,
  protein_id_column = "Protein.Group",
  seqinr_obj = aa_seq_tbl_final,
  seqinr_accession_column = "uniprot_acc"
)
}

}
