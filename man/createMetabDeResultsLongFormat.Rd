% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/func_metab_de.R, R/func_lipid_de.R
\name{createMetabDeResultsLongFormat}
\alias{createMetabDeResultsLongFormat}
\title{Create metabolomics DE results in long format with sample intensity columns}
\usage{
createMetabDeResultsLongFormat(
  lfc_qval_tbl,
  expr_matrix,
  design_matrix,
  sample_id_col = "sample_id",
  group_id_col = "group",
  lipid_id_col = "lipid_id"
)

createMetabDeResultsLongFormat(
  lfc_qval_tbl,
  expr_matrix,
  design_matrix,
  sample_id_col = "sample_id",
  group_id_col = "group",
  lipid_id_col = "lipid_id"
)
}
\arguments{
\item{lfc_qval_tbl}{Data frame with DE statistics (logFC, pvalue, fdr_qvalue).
Must have a lipid_id column and a comparison column containing the
contrast string in format "groupA-groupB".}

\item{expr_matrix}{Expression matrix with lipids as rows, samples as columns.
Rownames must match lipid_id column in lfc_qval_tbl.}

\item{design_matrix}{Design matrix with sample and group assignments.}

\item{sample_id_col}{Name of sample ID column in design_matrix.}

\item{group_id_col}{Name of group ID column in design_matrix.}

\item{lipid_id_col}{Name of lipid ID column.}

\item{metabolite_id_col}{Name of metabolite ID column.}
}
\value{
Data frame with DE results plus intensity columns for each sample.

Data frame with DE results plus intensity columns for each sample.
}
\description{
Creates a long-format DE results table that includes individual
sample intensity values, mirroring the proteomics \code{createDeResultsLongFormat()}.
The output includes columns for each sample's intensity value, named as
\verb{intensity.\{sample_id\}.\{group\}}.

Creates a long-format DE results table that includes individual
sample intensity values, mirroring the proteomics \code{createDeResultsLongFormat()}.
The output includes columns for each sample's intensity value, named as
\verb{intensity.\{sample_id\}.\{group\}}.
}
