% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R, R/func_general_filemgmt.R
\name{setupDirectories}
\alias{setupDirectories}
\title{Setup directories}
\usage{
setupDirectories(
  base_dir = here::here(),
  omic_types,
  label = NULL,
  force = FALSE,
  reuse_existing = FALSE
)

setupDirectories(
  base_dir = here::here(),
  omic_types,
  label = NULL,
  force = FALSE,
  reuse_existing = FALSE
)
}
\arguments{
\item{base_dir}{Base directory path (optional, defaults to here::here())}

\item{omic_types}{Character vector or comma/space-separated string indicating the types of omics data (e.g., c("proteomics", "metabolomics"), "proteomics, transcriptomics").}

\item{label}{Optional label to append to each omic type directory name (e.g., "proteomics_MyLabel").}

\item{force}{Logical; if TRUE, skips user confirmation for overwriting existing directories (default: FALSE).}

\item{reuse_existing}{Logical; if TRUE, uses an existing directory structure without prompting or overwriting (default: FALSE). For non-interactive use.}
}
\value{
A named list where each name is an omic type. Each element is a list containing the relevant directory paths for that omic type.

A named list where each name is an omic type. Each element is a list containing the relevant directory paths for that omic type.
}
\description{
Creates and manages project directories with version control for multiple omic types.

Creates and manages project directories with version control for multiple omic types.
}
