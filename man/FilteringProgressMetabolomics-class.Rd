% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QC_visualisation.R
\docType{class}
\name{FilteringProgressMetabolomics-class}
\alias{FilteringProgressMetabolomics-class}
\title{S4 Class Definition for Metabolomics Filtering Progress}
\description{
An S4 class to store QC metrics collected at different steps
of a metabolomics data processing workflow.
}
\section{Slots}{

\describe{
\item{\code{steps}}{A character vector storing the names of the filtering steps.}

\item{\code{assay_names}}{A list where each element corresponds to a step and contains
a character vector of the original assay names for that step.}

\item{\code{n_metabolites_per_assay}}{A list where each element corresponds to a step.
Each element is a list itself, containing named
numeric vectors (assay_name = count of unique metabolites).}

\item{\code{n_metabolites_total}}{A numeric vector storing the total unique metabolites
(across all assays) identified at each step.}

\item{\code{detected_per_sample}}{A list where each element corresponds to a step.
Each element is a list (per assay) containing data frames
with columns for sample ID (e.g., 'Run') and the number
of detected metabolites ('n_detected').}

\item{\code{missingness_per_assay}}{A list where each element corresponds to a step.
Each element is a list containing named numeric
vectors (assay_name = percentage missing).}

\item{\code{sum_intensity_per_sample}}{A list where each element corresponds to a step.
Each element is a list (per assay) containing data frames
with columns for sample ID ('Run') and the total
intensity ('sum_intensity').}

\item{\code{cv_distribution_per_assay}}{A list where each element corresponds to a step.
Each element is a list (per assay) containing data frames
with columns for metabolite ID, group, and calculated
coefficient of variation ('cv').}

\item{\code{is_metrics_per_assay}}{A list where each element corresponds to a step.
Each element is a list (per assay) containing data frames
summarizing internal standard metrics (e.g., 'is_id',
'metric_name', 'value').}
}}

\keyword{internal}
