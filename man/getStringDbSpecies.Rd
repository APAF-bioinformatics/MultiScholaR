% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/func_multiomics_enrich.R,
%   R/multiomics_enrichment_functions.R
\name{getStringDbSpecies}
\alias{getStringDbSpecies}
\title{Get Available Species from STRING DB}
\usage{
getStringDbSpecies(search_term = NULL, api_key = NULL)

getStringDbSpecies(search_term = NULL, api_key = NULL)
}
\arguments{
\item{search_term}{Character string. Optional search term to filter species
by name (case-insensitive partial matching). If NULL, returns all species.
Default: NULL.}

\item{api_key}{Character string. Your STRING DB API key. Default: NULL.}
}
\value{
A data frame containing available species with columns:
\itemize{
\item species_id: STRING DB species identifier (e.g., "9606", "STRG0A62HCE")
\item official_name: Official species name
\item compact_name: Compact species name
\item taxon_id: NCBI taxonomy ID
}

A data frame containing available species with columns:
\itemize{
\item species_id: STRING DB species identifier (e.g., "9606", "STRG0A62HCE")
\item official_name: Official species name
\item compact_name: Compact species name
\item taxon_id: NCBI taxonomy ID
}
}
\description{
This function queries the STRING DB API to retrieve all available species
and their corresponding species identifiers. This is useful for finding
the correct species code to use in STRING DB enrichment analyses.

This function queries the STRING DB API to retrieve all available species
and their corresponding species identifiers. This is useful for finding
the correct species code to use in STRING DB enrichment analyses.
}
\examples{
\dontrun{
# Get all available species
all_species <- getStringDbSpecies()

# Search for human species
human_species <- getStringDbSpecies("sapiens")

# Search for mouse species  
mouse_species <- getStringDbSpecies("musculus")

# Search for a specific genus
zingiber_species <- getStringDbSpecies("zingiber")
}
\dontrun{
# Get all available species
all_species <- getStringDbSpecies()

# Search for human species
human_species <- getStringDbSpecies("sapiens")

# Search for mouse species  
mouse_species <- getStringDbSpecies("musculus")

# Search for a specific genus
zingiber_species <- getStringDbSpecies("zingiber")
}
}
