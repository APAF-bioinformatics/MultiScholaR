% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/func_metab_import.R, R/func_metab_s4_objects.R
\name{createMetaboliteAssayData}
\alias{createMetaboliteAssayData}
\title{Create MetaboliteAssayData Object}
\usage{
createMetaboliteAssayData(
  metabolite_data,
  design_matrix,
  metabolite_id_column = "database_identifier",
  annotation_id_column = "metabolite_identification",
  sample_id = "Sample_ID",
  group_id = "group",
  technical_replicate_id = NA_character_,
  database_identifier_type = "Unknown",
  internal_standard_regex = NA_character_,
  args = list()
)

createMetaboliteAssayData(
  metabolite_data,
  design_matrix,
  metabolite_id_column = "database_identifier",
  annotation_id_column = "metabolite_identification",
  sample_id = "Sample_ID",
  group_id = "group",
  technical_replicate_id = NA_character_,
  database_identifier_type = "Unknown",
  internal_standard_regex = NA_character_,
  args = list()
)
}
\arguments{
\item{metabolite_data}{Named list of data frames (assays).}

\item{design_matrix}{Experimental design data frame.}

\item{metabolite_id_column}{Name of the \strong{primary feature ID} column within assays (e.g., \code{"database_identifier"}).}

\item{annotation_id_column}{Name of the \strong{annotation ID} column (e.g., \code{"metabolite_identification"}).}

\item{sample_id}{Name of the sample ID column in design_matrix and assays.}

\item{group_id}{Name of the group column in design_matrix.}

\item{technical_replicate_id}{Name of the technical replicate column in design_matrix (use NA_character_ if none).}

\item{database_identifier_type}{Type of identifier in the \code{annotation_id_column} (e.g., \code{"Mixed_CHEBI_Unknown"}).}

\item{internal_standard_regex}{Regex to identify internal standards. Use \code{NA_character_} or \code{""} if none.}

\item{args}{List of arguments (e.g., from config).}
}
\value{
A MetaboliteAssayData object.

A MetaboliteAssayData object.
}
\description{
Constructor function for the MetaboliteAssayData class.

Constructor function for the MetaboliteAssayData class.
}
\examples{
\dontrun{
# Assuming lcms_pos_df, lcms_neg_df, gcms_df are data frames
# with 'Metabolite' as ID column and samples as other columns
# Assuming design_df has 'SampleID', 'Group', 'Replicate' columns
assays_list <- list(
    LCMS_Pos = lcms_pos_df,
    LCMS_Neg = lcms_neg_df,
    GCMS = gcms_df
)
config <- list(...) # Your config list

met_assay_obj <- createMetaboliteAssayData(
    metabolite_data = assays_list,
    design_matrix = design_df,
    metabolite_id_column = "Metabolite",
    sample_id = "SampleID",
    group_id = "Group",
    technical_replicate_id = "Replicate",
    database_identifier_type = "InternalName",
    internal_standard_regex = "^IS_",
    args = config
)
}
\dontrun{
# Assuming lcms_pos_df, lcms_neg_df, gcms_df are data frames
# with 'Metabolite' as ID column and samples as other columns
# Assuming design_df has 'SampleID', 'Group', 'Replicate' columns
assays_list <- list(
    LCMS_Pos = lcms_pos_df,
    LCMS_Neg = lcms_neg_df,
    GCMS = gcms_df
)
config <- list(...) # Your config list

met_assay_obj <- createMetaboliteAssayData(
    metabolite_data = assays_list,
    design_matrix = design_df,
    metabolite_id_column = "Metabolite",
    sample_id = "SampleID",
    group_id = "Group",
    technical_replicate_id = "Replicate",
    database_identifier_type = "InternalName",
    internal_standard_regex = "^IS_",
    args = config
)
}
}
