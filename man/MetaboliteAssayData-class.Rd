% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/func_metab_s4_objects.R
\docType{class}
\name{MetaboliteAssayData-class}
\alias{MetaboliteAssayData-class}
\alias{MetaboliteAssayData}
\title{Metabolite Assay Data S4 Class}
\description{
An S4 class to store and manage multiple metabolomics quantitative datasets
derived from different platforms or ionization modes, along with associated
experimental design and metadata.
}
\section{Slots}{

\describe{
\item{\code{metabolite_data}}{A named list of data frames. Each data frame contains
quantitative data for one assay (e.g., LCMS-Pos). Rows should represent
metabolites/features, and columns should represent samples. Each data frame
MUST contain the column specified by \code{metabolite_id_column}. The names of
the list elements should describe the assay source (e.g., "LCMS_Pos").}

\item{\code{metabolite_id_column}}{Character string. The name of the column within
each assay data frame that contains the \strong{primary feature identifier} (used
for uniquely identifying rows in quantitative analysis).}

\item{\code{annotation_id_column}}{Character string. The name of the column (often
in feature metadata, but could be in assay tables) that contains the
\strong{biological or database annotation identifier} (e.g., HMDB ID, KEGG ID,
chemical name, CHEBI ID). This is the ID typically used for downstream
biological mapping.}

\item{\code{database_identifier_type}}{Character string. Describes the type or format
of identifiers found in the column specified by \code{annotation_id_column}
(e.g., "HMDB", "KEGG", "CHEBI", "Mixed_CHEBI_Unknown", "InternalName").}

\item{\code{internal_standard_regex}}{Character string. A regular expression used to
identify features that are internal standards based on their identifier in
the \code{metabolite_id_column} or \code{annotation_id_column}. Set to \code{NA_character_} or \code{""}
if not applicable or no internal standards are used.}

\item{\code{design_matrix}}{A data frame containing the experimental design. Must
include the column specified by \code{sample_id}.}

\item{\code{sample_id}}{Character string. The name of the column in \code{design_matrix}
that contains unique sample identifiers. These identifiers must correspond
to the sample column names in the assay data frames.}

\item{\code{group_id}}{Character string. The name of the column in \code{design_matrix}
that defines the primary experimental groups or conditions.}

\item{\code{technical_replicate_id}}{Character string. The name of the column in
\code{design_matrix} that identifies technical replicates, if applicable. Use
\code{NA_character_} if there are no technical replicates.}

\item{\code{args}}{A list, typically populated from a configuration file, holding
parameters used during processing.}
}}

