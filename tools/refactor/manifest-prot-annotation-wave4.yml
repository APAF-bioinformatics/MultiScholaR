version: 1
wave: wave4_proteomics_annotation_accession_ranking_helpers
source_root: R
collate_mode: explicit

defaults:
  action: extract
  preserve:
    roxygen: true
    comments: true
    whitespace: true

collate_targets:
  - R/func_prot_annotation_accession_ranking_helpers.R

entries:
  - id: prot_annotation_choose_best_protein_accession_helper
    source: R/func_prot_annotation.R
    selector: { kind: symbol, value: chooseBestProteinAccessionHelper }
    target: R/func_prot_annotation_accession_ranking_helpers.R
    group: proteomics.annotation.accession_ranking
    note: Stage the fourth exact-source annotation wave around chooseBestProteinAccessionHelper() and rankProteinAccessionHelper() while keeping updateProteinIDs(), cleanMaxQuantProteins(), and the phosphosite helper block in the main annotation file for a later checkpoint.
  - id: prot_annotation_rank_protein_accession_helper
    source: R/func_prot_annotation.R
    selector: { kind: symbol, value: rankProteinAccessionHelper }
    target: R/func_prot_annotation_accession_ranking_helpers.R
    group: proteomics.annotation.accession_ranking
    note: Keep the accession-record update path and downstream phosphosite/accession cleanup helpers in func_prot_annotation.R for a later checkpoint.
