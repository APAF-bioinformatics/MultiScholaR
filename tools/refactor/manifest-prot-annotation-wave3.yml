version: 1
wave: wave3_proteomics_annotation_fasta_processing_helper
source_root: R
collate_mode: explicit

defaults:
  action: extract
  preserve:
    roxygen: true
    comments: true
    whitespace: true

collate_targets:
  - R/func_prot_annotation_fasta_processing_helpers.R

entries:
  - id: prot_annotation_process_fasta_file
    source: R/func_prot_annotation.R
    selector: { kind: symbol, value: processFastaFile }
    target: R/func_prot_annotation_fasta_processing_helpers.R
    group: proteomics.annotation.fasta
    note: Stage the third exact-source annotation wave around processFastaFile() while keeping chooseBestProteinAccessionHelper(), rankProteinAccessionHelper(), and the phosphosite/accession-ranking blocks in the main annotation file for a later checkpoint.
