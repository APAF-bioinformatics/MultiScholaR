version: 1
wave: wave4_proteomics_peptide_s4_plot_pca
source_root: R
collate_mode: explicit

defaults:
  action: extract
  preserve:
    roxygen: true
    comments: true
    whitespace: true

collate_targets:
  - R/func_pept_s4_qc_methods.R

entries:
  - id: pept_s4_qc_method_plot_pca_legacy
    source: R/func_pept_s4_objects.R
    selector:
      kind: anchor_range
      start: "^# Renamed from plotPca to avoid S4 generic conflict$"
      end: "^# Set up S4 class definitions for ggplot objects$"
      end_inclusive: false
    target: R/func_pept_s4_qc_methods.R
    group: proteomics.peptide.s4.qc_methods
    note: Fourth staged peptide S4 wave moves the legacy peptide plotPca dispatch block plus its exact legacy PeptideQuantitativeData method into the existing peptide QC helper without rewriting live sources.
  - id: pept_s4_qc_method_plot_pca_active
    source: R/func_pept_s4_objects.R
    selector:
      kind: anchor_range
      start: "^# PCA plot for PeptideQuantitativeData$"
      end: "^# Density plot for PeptideQuantitativeData$"
      end_inclusive: false
    target: R/func_pept_s4_qc_methods.R
    group: proteomics.peptide.s4.qc_methods
    note: The same wave also moves the currently active exact plotPca PeptideQuantitativeData method block so the helper preserves the wrapper's current load-order behavior.
