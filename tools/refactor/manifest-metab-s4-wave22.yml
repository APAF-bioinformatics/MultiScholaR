version: 1
wave: wave22_metabolomics_s4_resolve_duplicate_features_by_intensity
source_root: R
collate_mode: explicit

defaults:
  action: extract
  preserve:
    roxygen: true
    comments: true
    whitespace: true

collate_targets:
  - R/func_metab_s4_qc_methods.R

entries:
  - id: metab_s4_qc_helper_resolve_duplicate_features_by_intensity
    source: R/func_metab_s4_objects.R
    selector: { kind: symbol, value: resolveDuplicateFeaturesByIntensity }
    target: R/func_metab_s4_qc_methods.R
    group: metabolomics.s4.qc.methods
    note: Wave 22 stages the bounded metabolomics S4 resolveDuplicateFeaturesByIntensity helper checkpoint into the live helper file without rewriting the function body.
